
var vec3 = require('gl-vec3')


export default function (noa) {
    return {

        name: 'canon',

        order: 100,

        state: {
            mesh: null,
            offset: [0,0,0]
			
		
			
			
			
        },


        onAdd: function (eid, state) {
			state.id=eid
            state.mesh=noa.ents.getState(state.id, 'entmesh').mesh
			
			setInterval(function(){ 
			//state.mesh._children[0].rotation.y=noa.camera.heading;
state.mesh.rotation.y=noa.camera.heading;
//state.mesh.rotation.x=-noa.camera.pitch;
			}, 500);
	 
			
        }


		
		


    }
}
